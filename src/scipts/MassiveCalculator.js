export default class MassiveCalulator {
    constructor() {
        this.classVar = "massive calculation that only needs javascript and is used in multiple places"
    }

    print() {
        console.log(this.classVar, "2+2");
    }
}